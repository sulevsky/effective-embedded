[tools]
# rust = "latest"

[tasks.run]
run = "cargo run"

[tasks.build]
alias = "b"
run = "cargo build"


[tasks.flash]
alias = "f"
run = "cargo run --target xtensa-esp32s3-none-elf"

[tasks.flash-release]
run = "cargo run --target xtensa-esp32s3-none-elf --release"


[env]
HELLO = "hello"
